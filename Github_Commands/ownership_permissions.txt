file ownership and modes refers to the level of 0f access or who exactly has or can access a file or directory. thus, these are permissions that give the owner, group or others the reading, writing and executing right over the file or directory. reading is represented with r, writing is w and execute is x. these representations appear to diplay the user's level of access to the directory or file.